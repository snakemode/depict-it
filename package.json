{
  "name": "static-app",
  "version": "",
  "description": "",
  "repository": "http://tempuri.org",
  "license": "http://tempuri.org",
  "scripts": {
    "start": "run-p dev:server dev:api",
    "test": "jest",
    "test:acceptance": "jest acceptance",
    "dev:server": "npx live-server app --proxy=/api:http://127.0.0.1:7071/api",
    "dev:server:test": "npx live-server app --proxy=/api:http://127.0.0.1:7071/api --no-browser --quiet",
    "dev:api": "npm run start --prefix api",

    "dev:server:acceptance": "run-p dev:server:test dev:api test:acceptance",
    "dev:server:quit": "npx kill-port 8080",
    "dev:api:quit": "npx kill-port 7071",
    "acceptance": "run-s dev:server:acceptance dev:server:quit dev:api:quit",

    "build:azure": ""
  },
  "author": "",
  "dependencies": {},
  "devDependencies": {
    "@babel/cli": "^7.10.4",
    "@babel/core": "^7.10.4",
    "@babel/preset-env": "^7.10.4",
    "jest": "^26.1.0",
    "jest-cli": "^26.1.0",
    "live-server": "^1.2.1",
    "npm-run-all": "^4.1.5",
    "playwright": "^1.2.1"
  }
}
